#include <stdio.h>
#include<string.h>
/*int main(){
    char p[]="programiz";

    printf("%s",p);
}



int main(){
    char p [20];// an array of character and size 20 letters
    printf("Enter your name:");
    scanf("%s",p);// we dont put a pointer because it is an array
    printf("%s",p);
    return 0;
}


int main(){

    char m []="Programming";
    printf("%s",m);


    printf("\nlength: %zu",strlen(m)); //  zu is a place holder for strlen and it is used to measure the length of strings
    return 0;
}
//strcpy()function is used to copy one function to another

int main(){
    char food[]="pizza";
    char bestFood[strlen(food)];//this copies the length of the food to the bestfood
    strcpy(bestFood,food);
    printf("%s",bestFood);
    return 0;
}
//strcat function is used to join 2 strings together

int main(){
    char m[]="Hey, ";
    char n[]="How are you?";
    strcat(m,n);
    printf("%s",m);
    return 0;
}*/
//strcmp is used to compare strings if they are the same they bring back the 0 but if not it brings a random non zero number